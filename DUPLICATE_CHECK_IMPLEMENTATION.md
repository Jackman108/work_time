# Реализация проверки дублей в учёте рабочего времени

## ✅ Выполнено

### Требование
В учёте заработанных денег работниками:
- ✅ Один человек может работать в один день на разных объектах
- ✅ НЕ должно быть 2 записей в один день на одном объекте

### Реализация

#### 1. Серверная проверка (`services/workLog.js`)

Добавлена функция `checkDuplicate()`:
```javascript
function checkDuplicate(employeeId, projectId, date, excludeId = null)
```

- Проверяет наличие записи с комбинацией: `employee_id + project_id + date`
- При обновлении исключает текущую запись (`excludeId`)
- Выбрасывает `ValidationError` с понятным сообщением

**Использование:**
- В `createWorkLog()` - проверка перед созданием
- В `updateWorkLog()` - проверка перед обновлением (исключая текущую запись)

#### 2. Клиентская проверка (`renderer/components/WorkLogForm.jsx`)

Добавлена проверка на клиенте перед отправкой:
- Использует функцию `checkDuplicateByCombination()` из `templates.js`
- Проверяет комбинацию: `employee_id + project_id + date`
- Показывает ошибку в поле "Дата" с понятным сообщением
- Предотвращает отправку формы при наличии дубля

#### 3. Уникальный индекс в БД (`database.js`)

Добавлен уникальный индекс:
```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_work_log_unique 
ON work_log(employee_id, project_id, date)
```

**Преимущества:**
- Защита на уровне базы данных
- Невозможно создать дубль даже при обходе проверок приложения
- Автоматическая очистка существующих дублей при миграции

#### 4. Утилита для проверки комбинации (`renderer/utils/templates.js`)

Добавлена функция `checkDuplicateByCombination()`:
```javascript
checkDuplicateByCombination(data, existingItems, fieldsToCheck, excludeId)
```

- Проверяет уникальность комбинации нескольких полей
- Поддерживает исключение записи при обновлении
- Возвращает понятное сообщение об ошибке

## 🎯 Логика работы

### При создании записи:
1. Пользователь заполняет форму (сотрудник, проект, дата, зарплата)
2. Клиентская проверка: есть ли уже запись с такой комбинацией?
3. Если есть - показывается ошибка, форма не отправляется
4. Если нет - данные отправляются на сервер
5. Серверная проверка: повторная проверка на дубли
6. Если есть - выбрасывается ValidationError
7. Если нет - запись создаётся
8. БД проверка: уникальный индекс предотвращает дубли на уровне БД

### При обновлении записи:
1. Пользователь изменяет запись
2. Клиентская проверка: есть ли другая запись (не текущая) с такой комбинацией?
3. Если есть - показывается ошибка
4. Если нет - данные отправляются на сервер
5. Серверная проверка: повторная проверка (исключая текущую запись)
6. Если есть - выбрасывается ValidationError
7. Если нет - запись обновляется

## 📋 Примеры

### Разрешённые сценарии:
✅ Сотрудник Иван работает 2024-01-15 на объекте "Дом 1" - **OK**
✅ Сотрудник Иван работает 2024-01-15 на объекте "Дом 2" - **OK** (другой объект)
✅ Сотрудник Иван работает 2024-01-16 на объекте "Дом 1" - **OK** (другая дата)

### Запрещённые сценарии:
❌ Сотрудник Иван работает 2024-01-15 на объекте "Дом 1" - **OK**
❌ Сотрудник Иван работает 2024-01-15 на объекте "Дом 1" - **ОШИБКА** (дубль)

## 🔒 Многоуровневая защита

1. **Клиентская проверка** - мгновенная обратная связь, не отправляет форму
2. **Серверная проверка** - проверка на сервере перед записью в БД
3. **Уникальный индекс БД** - защита на уровне базы данных

## ✅ Результат

Теперь система гарантирует, что:
- Один сотрудник может работать в один день на разных объектах
- Невозможно создать две записи для одного сотрудника на одном объекте в один день
- Пользователь получает понятное сообщение об ошибке
- Защита работает на трёх уровнях (клиент, сервер, БД)

