# Реализация валидации форм на клиенте

## ✅ Выполнено

### 1. Система валидации (`renderer/utils/formValidator.js`)

Реализован класс `FormValidator` с методами:
- ✅ `validateRequired` - проверка обязательных полей
- ✅ `validateNumber` - валидация чисел (min, max, allowZero, allowNegative)
- ✅ `validateString` - валидация строк (длина, паттерны)
- ✅ `validateDate` - валидация дат
- ✅ `validatePhone` - валидация телефонов
- ✅ `validateDateRange` - проверка логики дат (дата окончания >= дата начала)
- ✅ `validateForm` - комплексная валидация формы по правилам

### 2. Визуальная индикация ошибок (`renderer/components/FormErrors.jsx`)

Созданы компоненты:
- ✅ `FormErrors` - отображение общих ошибок и ошибок по полям
- ✅ `FieldError` - отображение ошибки конкретного поля
- ✅ `getFieldClasses` - получение CSS классов для полей с ошибками (is-invalid)

### 3. Автозаполнение (`renderer/utils/autocomplete.js`)

Реализованы функции:
- ✅ `saveToHistory` - сохранение значений в историю (localStorage)
- ✅ `getHistory` - получение истории значений
- ✅ `getSuggestions` - получение подсказок по введённому тексту
- ✅ `clearHistory` - очистка истории

### 4. Шаблоны (`renderer/utils/templates.js`)

Реализованы функции:
- ✅ `getTemplates` - получение шаблонов для типа формы
- ✅ `saveTemplate` - сохранение шаблона
- ✅ `applyTemplate` - применение шаблона к форме
- ✅ `deleteTemplate` - удаление шаблона
- ✅ `checkDuplicates` - проверка наличия дублей

### 5. Обновлённые формы

Все формы обновлены с валидацией:

#### ProjectForm
- ✅ Валидация обязательных полей (название)
- ✅ Валидация дат (проверка логики: дата окончания >= дата начала)
- ✅ Валидация бюджета (неотрицательное число)
- ✅ Автозаполнение для названия и адреса
- ✅ Шаблоны проектов
- ✅ Проверка дублей по названию
- ✅ Визуальная индикация ошибок

#### EmployeeForm
- ✅ Валидация обязательных полей (ФИО)
- ✅ Валидация телефона
- ✅ Валидация ставки за час (неотрицательное число)
- ✅ Автозаполнение для ФИО, должности, телефона
- ✅ Шаблоны сотрудников
- ✅ Проверка дублей по ФИО
- ✅ Визуальная индикация ошибок

#### MaterialForm
- ✅ Валидация обязательных полей (название)
- ✅ Валидация цены (неотрицательное число)
- ✅ Автозаполнение для названия
- ✅ Шаблоны материалов
- ✅ Проверка дублей по названию
- ✅ Визуальная индикация ошибок

#### WorkLogForm
- ✅ Валидация обязательных полей (сотрудник, проект, дата, зарплата)
- ✅ Валидация зарплаты (положительное число)
- ✅ Валидация даты
- ✅ Визуальная индикация ошибок

#### MaterialLogForm
- ✅ Валидация обязательных полей (материал, проект, дата, количество)
- ✅ Валидация количества (положительное число)
- ✅ Валидация даты
- ✅ Визуальная индикация ошибок

#### ProjectPaymentForm
- ✅ Валидация обязательных полей (проект, дата, сумма)
- ✅ Валидация суммы (минимум 100, кратно 100)
- ✅ Валидация даты
- ✅ Визуальная индикация ошибок

## 🎯 Функционал

### Валидация в реальном времени
- Ошибки очищаются при изменении поля
- Валидация дат происходит при изменении обеих дат
- Визуальная индикация через класс `is-invalid`

### Автозаполнение
- Подсказки появляются при вводе 2+ символов
- История сохраняется в localStorage
- Максимум 20 значений в истории
- Подсказки отображаются в выпадающем списке

### Шаблоны
- Сохранение текущих данных формы как шаблон
- Применение сохранённых шаблонов
- Шаблоны сохраняются в localStorage
- Максимум 50 шаблонов на тип формы

### Проверка дублей
- Автоматическая проверка при создании новой записи
- Проверка по указанным полям (например, название проекта)
- Понятные сообщения об ошибках

### Визуальная индикация
- Красная рамка у полей с ошибками (Bootstrap `is-invalid`)
- Текст ошибки под полем
- Общий блок с ошибками вверху формы
- Список всех ошибок для удобства

## 📋 Примеры использования

### Валидация формы

```javascript
const rules = {
  required: ['name', 'email'],
  fields: {
    name: {
      type: 'string',
      label: 'Имя',
      required: true,
      minLength: 2,
      maxLength: 255
    },
    email: {
      type: 'string',
      label: 'Email',
      required: true,
      pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/
    },
    age: {
      type: 'number',
      label: 'Возраст',
      required: false,
      min: 0,
      max: 120
    }
  },
  custom: [
    {
      field: 'date_end',
      validator: (data) => {
        if (data.date_start && data.date_end) {
          return FormValidator.validateDateRange(data.date_start, data.date_end);
        }
        return { isValid: true };
      }
    }
  ]
};

const validation = FormValidator.validateForm(form, rules);
if (!validation.isValid) {
  setErrors(validation.errors);
  return;
}
```

### Использование автозаполнения

```javascript
// Сохранение в историю
saveToHistory('name', form.name);

// Получение подсказок
const suggestions = getSuggestions('name', inputValue);
```

### Использование шаблонов

```javascript
// Сохранение шаблона
saveTemplate('projects', 'Типовой проект', form);

// Применение шаблона
const templateData = applyTemplate('projects', 'Типовой проект');
if (templateData) {
  setForm({ ...form, ...templateData });
}
```

### Проверка дублей

```javascript
const duplicateCheck = checkDuplicates(
  'projects',
  form,
  existingProjects,
  ['name']
);

if (duplicateCheck.hasDuplicate) {
  setErrors({ name: duplicateCheck.message });
  return;
}
```

## 🎨 Визуальные улучшения

1. **Красная рамка** у полей с ошибками (Bootstrap класс `is-invalid`)
2. **Текст ошибки** под каждым полем
3. **Общий блок ошибок** вверху формы со списком всех ошибок
4. **Выпадающий список** подсказок при автозаполнении
5. **Панель шаблонов** для быстрого применения сохранённых данных

## ✅ Результат

Все формы теперь имеют:
- ✅ Полную валидацию на клиенте
- ✅ Визуальную индикацию ошибок
- ✅ Автозаполнение для текстовых полей
- ✅ Шаблоны для часто используемых данных (проекты, сотрудники, материалы)
- ✅ Проверку дублей
- ✅ Проверку логики (даты, числа, форматы)

Пользователь получает мгновенную обратную связь при заполнении форм, что значительно улучшает UX.

