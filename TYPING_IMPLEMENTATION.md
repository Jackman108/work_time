# Типизация проекта

## 📋 Обзор

Проект типизирован с использованием **JSDoc** типов и **TypeScript** для проверки типов. Это позволяет получить преимущества типизации без необходимости переписывать весь код на TypeScript.

## 🎯 Преимущества

1. **Автодополнение в IDE** - IntelliSense понимает типы
2. **Проверка типов** - TypeScript проверяет корректность использования типов
3. **Документация** - Типы служат документацией для разработчиков
4. **Раннее обнаружение ошибок** - Ошибки типов обнаруживаются до запуска

## 📁 Структура типов

```
types/
└── index.d.ts          # Централизованные определения типов
```

## 🔧 Настройка

### 1. Установка зависимостей

```bash
npm install --save-dev typescript
```

### 2. Конфигурация TypeScript

Файл `tsconfig.json` настроен для:
- Проверки JavaScript файлов (`checkJs: true`)
- Без генерации файлов (`noEmit: true`)
- Поддержки React JSX
- Использования типов из `types/`

### 3. Проверка типов

```bash
npm run type-check
```

## 📝 Использование типов

### В JSDoc комментариях

```javascript
/**
 * Получить все проекты
 * @returns {Promise<Project[]>} Список всех проектов
 */
export async function getProjects() {
  // ...
}
```

### Типы параметров

```javascript
/**
 * Создать новый проект
 * @param {ProjectCreateData} data - Данные проекта
 * @returns {Promise<Project>} Созданный проект
 */
export async function createProject(data) {
  // ...
}
```

### Типы компонентов

```javascript
/**
 * Форма для добавления/редактирования проекта
 * @param {FormProps & { project?: Project, existingProjects?: Project[] }} props - Пропсы компонента
 */
export default function ProjectForm({ project, onSave, onCancel, existingProjects = [] }) {
  // ...
}
```

## 📚 Определённые типы

### Сущности данных

- `Project` - Проект
- `Employee` - Сотрудник
- `Material` - Материал
- `WorkLog` - Запись рабочего времени
- `MaterialLog` - Запись списания материалов
- `ProjectPayment` - Поступление денег на проект

### Данные для создания/обновления

- `ProjectCreateData` / `ProjectUpdateData`
- `EmployeeCreateData` / `EmployeeUpdateData`
- `MaterialCreateData` / `MaterialUpdateData`
- И т.д.

### Статистика и отчёты

- `ProjectStats` - Статистика проекта
- `EmployeeStats` - Статистика сотрудника
- `MaterialStats` - Статистика материала
- `OverallStats` - Общая статистика
- `ProjectReport` - Отчёт по проектам
- `EmployeeReport` - Отчёт по сотрудникам
- `MaterialReport` - Отчёт по материалам

### Фильтры

- `WorkLogFilters` - Фильтры для записей рабочего времени
- `MaterialLogFilters` - Фильтры для записей списания материалов
- `ProjectPaymentFilters` - Фильтры для поступлений

### Компоненты и хуки

- `FormProps` - Пропсы форм
- `ListProps` - Пропсы списков
- `UsePageDataConfig` / `UsePageDataReturn` - Конфигурация и результат хука usePageData
- `UseAsyncOperationOptions` / `UseAsyncOperationReturn` - Конфигурация и результат хука useAsyncOperation

### API

- `ApiResponse` - Структура ответа API

## ✅ Проверка типов

### В IDE

Большинство современных IDE (VS Code, WebStorm) автоматически используют TypeScript для проверки типов в JavaScript файлах при наличии `tsconfig.json`.

### Через командную строку

```bash
npm run type-check
```

Это проверит все файлы на соответствие типам без генерации файлов.

## 🔄 Миграция на TypeScript (опционально)

В будущем можно постепенно мигрировать проект на TypeScript:

1. Переименовать `.js` файлы в `.ts`
2. Добавить явные типы вместо JSDoc
3. Настроить компиляцию TypeScript

Но текущий подход с JSDoc даёт большинство преимуществ типизации без необходимости переписывать код.

## 📖 Дополнительная информация

Все типы определены в `types/index.d.ts` и доступны через JSDoc комментарии `@typedef` и `@param`, `@returns`.

