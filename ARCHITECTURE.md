# Архитектура приложения

## 📋 Оглавление

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Структура проекта](#структура-проекта)
3. [Принципы проектирования](#принципы-проектирования)
4. [Слои приложения](#слои-приложения)
5. [Паттерны проектирования](#паттерны-проектирования)
6. [Константы и конфигурация](#константы-и-конфигурация)
7. [Роутинг](#роутинг)
8. [Управление состоянием](#управление-состоянием)
9. [Обработка ошибок](#обработка-ошибок)
10. [Рекомендации для команды](#рекомендации-для-команды)

---

## Обзор архитектуры

Приложение построено на **Electron** с использованием **React** для UI и **SQLite** для хранения данных. Архитектура следует принципам **SOLID**, **DRY** и лучшим практикам разработки.

### Технологический стек

- **Backend**: Node.js (Electron Main Process)
- **Frontend**: React 18
- **Database**: SQLite (better-sqlite3)
- **UI Framework**: Bootstrap 5
- **Build Tool**: Vite

### Архитектурные принципы

1. **Разделение ответственности** - каждый модуль отвечает за одну задачу
2. **Единый источник истины** - константы и конфигурация централизованы
3. **Переиспользование кода** - базовые классы и хуки для устранения дублирования
4. **Типобезопасность** - использование JSDoc для документирования типов
5. **Расширяемость** - легко добавлять новые функции без изменения существующего кода

---

## Структура проекта

```
Electron/
├── config/                    # Конфигурация приложения
│   ├── app.config.js         # Основные настройки
│   └── ipc-channels.js       # Константы IPC каналов
├── services/                  # Бизнес-логика (Main Process)
│   ├── base/                  # Базовые классы
│   │   ├── BaseRepository.js # Базовый репозиторий
│   │   ├── ErrorHandler.js   # Обработка ошибок
│   │   └── Validator.js      # Валидация данных
│   ├── projects.js           # Сервис проектов
│   ├── employees.js          # Сервис сотрудников
│   └── ...                   # Другие сервисы
├── renderer/                  # Frontend (Renderer Process)
│   ├── api/                  # API для IPC вызовов
│   │   └── index.js          # Централизованный API
│   ├── components/           # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   └── ...              # Другие компоненты
│   ├── hooks/               # React хуки
│   │   ├── usePageData.js   # Хук для управления данными страницы
│   │   ├── useFilters.js    # Хук для фильтров
│   │   └── useAsyncOperation.js # Хук для async операций
│   ├── router/              # Роутинг
│   │   └── index.js         # Конфигурация маршрутов
│   ├── constants/           # Константы
│   │   └── index.js         # Константы приложения
│   ├── utils/               # Утилиты
│   └── App.jsx              # Главный компонент
├── database.js              # Подключение к БД
├── main.js                 # Главный процесс Electron
└── preload.js              # Preload скрипт
```

---

## Принципы проектирования

### SOLID

#### Single Responsibility Principle (SRP)
Каждый модуль отвечает за одну задачу:
- `BaseRepository` - только работа с БД
- `ErrorHandler` - только обработка ошибок
- `Validator` - только валидация данных
- `usePageData` - только управление данными страницы

#### Open/Closed Principle (OCP)
Система открыта для расширения, закрыта для модификации:
- Новые сервисы наследуются от `BaseRepository`
- Новые маршруты добавляются в `router/index.js` без изменения существующих
- Новые типы уведомлений добавляются в `NotificationSystem` без изменения кода

#### Liskov Substitution Principle (LSP)
Любой сервис может использовать `BaseRepository` без изменения логики:
```javascript
class ProjectsRepository extends BaseRepository {
  // Может переопределить методы, но интерфейс остаётся совместимым
}
```

#### Interface Segregation Principle (ISP)
Чёткие интерфейсы для каждого модуля:
- `usePageData` принимает только необходимые функции
- Компоненты получают только нужные пропсы

#### Dependency Inversion Principle (DIP)
Зависимости от абстракций:
- Сервисы зависят от `BaseRepository`, а не от конкретной реализации БД
- Компоненты используют хуки, а не напрямую API

### DRY (Don't Repeat Yourself)

Устранение дублирования через:
- `BaseRepository` - общие CRUD операции
- `usePageData` - общая логика страниц
- `useFilters` - общая логика фильтров
- Константы IPC каналов - единый источник истины

### Clean Code

- Понятные имена функций и переменных
- Комментарии JSDoc для всех публичных функций
- Единый стиль кода
- Разделение ответственности

---

## Слои приложения

### 1. Data Layer (Слой данных)

**Расположение**: `services/`, `database.js`

**Ответственность**:
- Работа с базой данных
- Валидация данных
- Обработка ошибок БД

**Компоненты**:
- `BaseRepository` - базовый класс для репозиториев
- `Validator` - валидация данных
- Конкретные сервисы (`projects.js`, `employees.js`, etc.)

**Пример использования**:
```javascript
const projectsRepository = new BaseRepository('projects');
const project = projectsRepository.getById(1);
```

### 2. Service Layer (Слой сервисов)

**Расположение**: `services/*.js`

**Ответственность**:
- Бизнес-логика
- Валидация бизнес-правил
- Обработка специфичных операций

**Пример**:
```javascript
function createProject(data) {
  // Валидация
  Validator.validateRequired(data, ['name', 'address']);
  // Проверка дублей
  if (checkDuplicate(data.name)) {
    throw new ValidationError('Проект с таким именем уже существует');
  }
  // Создание
  return projectsRepository.create(data);
}
```

### 3. IPC Layer (Слой межпроцессного взаимодействия)

**Расположение**: `main.js`, `preload.js`, `renderer/api/`

**Ответственность**:
- Коммуникация между Main и Renderer процессами
- Безопасность IPC вызовов
- Обработка ошибок IPC

**Компоненты**:
- `main.js` - регистрация IPC обработчиков
- `preload.js` - безопасный мост между процессами
- `renderer/api/index.js` - клиентский API

**Пример**:
```javascript
// Main process
registerIpcHandler(IPC_CHANNELS.PROJECTS.CREATE, (data) => 
  projectsService.createProject(data)
);

// Renderer process
const project = await createProject(data);
```

### 4. Presentation Layer (Слой представления)

**Расположение**: `renderer/components/`, `renderer/hooks/`

**Ответственность**:
- UI компоненты
- Управление состоянием компонентов
- Обработка пользовательских действий

**Компоненты**:
- Страницы (`pages/`)
- Формы (`*Form.jsx`)
- Списки (`*List.jsx`)
- Хуки (`hooks/`)

**Пример**:
```javascript
const { items, loading, handleAdd, handleDelete } = usePageData({
  loadData: () => getProjects(),
  createItem: createProject,
  deleteItem: deleteProject,
  messages: {
    createSuccess: 'Проект создан',
    deleteSuccess: 'Проект удалён'
  }
});
```

---

## Паттерны проектирования

### Repository Pattern

**Реализация**: `BaseRepository`

**Цель**: Абстракция доступа к данным

**Преимущества**:
- Единый интерфейс для работы с БД
- Легко тестировать (можно заменить на моки)
- Упрощает миграцию на другую БД

### Factory Pattern

**Реализация**: `registerIpcHandler` в `main.js`

**Цель**: Создание обработчиков IPC с единой обработкой ошибок

**Преимущества**:
- Единая обработка ошибок
- Упрощение регистрации обработчиков

### Observer Pattern

**Реализация**: `NotificationSystem`

**Цель**: Уведомления компонентов о событиях

**Преимущества**:
- Слабая связанность компонентов
- Легко добавлять новых подписчиков

### Hook Pattern

**Реализация**: `usePageData`, `useFilters`, `useAsyncOperation`

**Цель**: Переиспользование логики между компонентами

**Преимущества**:
- Устранение дублирования
- Легко тестировать
- Композиция логики

---

## Константы и конфигурация

### IPC Каналы

**Расположение**: `config/ipc-channels.js`

**Использование**:
```javascript
// Main process
registerIpcHandler(IPC_CHANNELS.PROJECTS.CREATE, handler);

// Renderer process
await safeInvoke(IPC_CHANNELS.PROJECTS.CREATE, data);
```

**Преимущества**:
- Единый источник истины
- Автодополнение в IDE
- Предотвращение опечаток

### Константы приложения

**Расположение**: `renderer/constants/index.js`

**Содержит**:
- Маршруты (`ROUTES`)
- Типы уведомлений (`NOTIFICATION_TYPES`)
- Настройки по умолчанию

---

## Роутинг

**Расположение**: `renderer/router/index.js`

**Использование**:
```javascript
import { getRouteComponent, ROUTES } from './router';

const PageComponent = getRouteComponent(ROUTES.PROJECTS);
```

**Преимущества**:
- Централизованная конфигурация маршрутов
- Легко добавлять новые маршруты
- Типобезопасность через константы

---

## Управление состоянием

### Локальное состояние компонентов

Используется `useState` для локального состояния компонента.

### Глобальное состояние

Используется Context API для:
- Уведомлений (`NotificationProvider`)
- Темизации (если будет добавлена)

### Состояние страниц

Используется хук `usePageData` для управления:
- Загрузкой данных
- CRUD операциями
- Состоянием загрузки

---

## Обработка ошибок

### Многоуровневая обработка

1. **Service Layer** - валидация и бизнес-правила
2. **IPC Layer** - обработка ошибок IPC
3. **Presentation Layer** - показ ошибок пользователю

### Централизованная обработка

**Расположение**: `services/base/ErrorHandler.js`

**Типы ошибок**:
- `ValidationError` - ошибки валидации
- `NotFoundError` - ресурс не найден
- `DatabaseError` - ошибки БД

### Показ ошибок пользователю

Используется `NotificationSystem` для показа ошибок:
```javascript
const { showError } = useNotifications();
showError('Произошла ошибка');
```

---

## Рекомендации для команды

### Добавление новой страницы

1. **Создать сервис** (если нужен):
   ```javascript
   // services/newEntity.js
   const BaseRepository = require('./base/BaseRepository');
   const repository = new BaseRepository('new_entity');
   
   function getAllNewEntities() {
     return repository.getAll();
   }
   
   module.exports = { getAllNewEntities };
   ```

2. **Добавить IPC каналы**:
   ```javascript
   // config/ipc-channels.js
   const NEW_ENTITY = {
     GET_ALL: 'newEntity:getAll',
     CREATE: 'newEntity:create'
   };
   ```

3. **Зарегистрировать обработчики**:
   ```javascript
   // main.js
   registerIpcHandler(IPC_CHANNELS.NEW_ENTITY.GET_ALL, () => 
     newEntityService.getAllNewEntities()
   );
   ```

4. **Добавить API методы**:
   ```javascript
   // renderer/api/index.js
   export async function getNewEntities() {
     return await safeInvoke(IPC_CHANNELS.NEW_ENTITY.GET_ALL);
   }
   ```

5. **Создать страницу**:
   ```javascript
   // renderer/components/pages/NewEntityPage.jsx
   import { usePageData } from '../../hooks';
   import { getNewEntities, createNewEntity } from '../../api';
   
   export default function NewEntityPage() {
     const { items, handleAdd, confirmDialog } = usePageData({
       loadData: getNewEntities,
       createItem: createNewEntity,
       messages: {
         createSuccess: 'Элемент создан'
       }
     });
     
     return (
       <>
         {confirmDialog}
         {/* UI */}
       </>
     );
   }
   ```

6. **Добавить маршрут**:
   ```javascript
   // renderer/router/index.js
   import NewEntityPage from '../components/pages/NewEntityPage';
   
   export const routes = {
     // ...
     [ROUTES.NEW_ENTITY]: {
       component: NewEntityPage,
       title: 'Новая сущность',
       icon: '📝'
     }
   };
   ```

### Стиль кода

1. **Именование**:
   - Компоненты: `PascalCase` (например, `ProjectForm`)
   - Функции: `camelCase` (например, `getProjects`)
   - Константы: `UPPER_SNAKE_CASE` (например, `IPC_CHANNELS`)

2. **Комментарии**:
   - JSDoc для всех публичных функций
   - Комментарии для сложной логики

3. **Импорты**:
   - Использовать централизованные экспорты из `index.js`
   - Группировать импорты (внешние, внутренние)

### Тестирование

1. **Unit тесты** для:
   - Сервисов
   - Утилит
   - Хуков

2. **Integration тесты** для:
   - IPC вызовов
   - Работы с БД

3. **E2E тесты** для:
   - Критичных пользовательских сценариев

### Документация

1. **JSDoc** для всех публичных функций
2. **README** для каждого модуля (если сложный)
3. **ARCHITECTURE.md** для общих принципов

---

## Заключение

Архитектура приложения построена на принципах SOLID, DRY и Clean Code. Она обеспечивает:

- ✅ Легкость поддержки
- ✅ Расширяемость
- ✅ Тестируемость
- ✅ Читаемость кода
- ✅ Единый стиль

Следуя этим принципам и рекомендациям, команда может эффективно разрабатывать и поддерживать приложение.

